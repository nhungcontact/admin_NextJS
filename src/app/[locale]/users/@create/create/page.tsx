"use client";

import UserCreateDialog from "@/components/users/UserCreateDialog";

export type UserCreatePageProps = object;

function UserCreatePage({}: UserCreatePageProps) {
  return <UserCreateDialog />;
}

export default UserCreatePage;
